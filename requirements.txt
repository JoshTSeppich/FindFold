# pip install -r requirements.txt
# The scrapers drive the Chrome you already have installed, so there is no
# separate browser download step.

aiohttp==3.9.1
playwright==1.40.0
beautifulsoup4==4.12.2
lxml>=4.9.4
tldextract==5.1.1
click==8.1.7
rich==13.7.0
anthropic>=0.25.0
python-dotenv>=1.0.0
